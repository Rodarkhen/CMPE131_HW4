1. It does NOT change the variable a after the function call.

2. It changes the variable b after the function call.

3. They differs because, in Python, integers are immutable and are passed by value, which copies the 
value and it doesn not affect the passed value when changing in a function itself, whereas dictionary 
are mutable data type and are passed by reference meaning that any changes made inside the called 
function, the dictionary variable is going to be affected the variable itself.

